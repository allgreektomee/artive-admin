# React 연재 보관 문서

`/dev` React 탭 목차에는 올리지 않고, 저장소에만 남겨 둔 참고용 원고입니다.

`devOutline`의 React 문서 목록은 `src/dev/content/react/*.md` 직계 파일만 포함하므로, 이 폴더 아래 파일은 자동으로 노출되지 않습니다.

| 파일 | 원래 공개 장 | 비고 |
|------|----------------|------|
| [`11-auth-flow.md`](./11-auth-flow.md) | 11장 로그인·인증 | 코드 참고는 `src/etc/pages/LoginPage.tsx` 등 |
| [`15-websocket-realtime.md`](./15-websocket-realtime.md) | 15장 WebSocket | 코드 참고는 `src/etc/hooks/useChatWebSocket.ts` 등 |
